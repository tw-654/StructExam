# 中文乱码问题解决方案

## 问题描述
在Docker环境中运行应用时，前端显示中文乱码。

## 根本原因
Alpine Linux容器默认没有设置正确的locale和字符编码环境变量，导致中文显示为乱码。

## 解决方案

### 1. 已修改的文件

#### Dockerfile配置修改：
- `Dockerfile.frontend` - 前端容器配置
- `Dockerfile.gateway` - 网关服务配置  
- `Dockerfile.exam-service` - 考试服务配置
- `Dockerfile.user-service` - 用户服务配置
- `Dockerfile.code-service` - 代码服务配置
- `Dockerfile.sandbox-node` - 沙箱节点配置

#### 配置文件修改：
- `docker-compose.yml` - MySQL容器配置优化
- `frontend/nginx.conf` - Nginx编码配置优化

### 2. 主要修改内容

#### 2.1 所有Dockerfile添加以下配置：

```dockerfile
# 安装时区支持并设置中文环境变量
RUN apk add --no-cache tzdata && \
    cp /usr/share/zoneinfo/Asia/Shanghai /etc/localtime && \
    echo "Asia/Shanghai" > /etc/timezone && \
    echo "export LANG=zh_CN.UTF-8" >> /etc/profile.d/locale.sh && \
    echo "export LANGUAGE=zh_CN:zh" >> /etc/profile.d/locale.sh && \
    echo "export LC_ALL=zh_CN.UTF-8" >> /etc/profile.d/locale.sh && \
    echo "zh_CN.UTF-8 UTF-8" > /etc/locale.alias

# 设置环境变量
ENV LANG=zh_CN.UTF-8 \
    LANGUAGE=zh_CN:zh \
    LC_ALL=zh_CN.UTF-8 \
    TZ=Asia/Shanghai
```

#### 2.2 Java服务额外添加JAVA_TOOL_OPTIONS：
```dockerfile
ENV JAVA_TOOL_OPTIONS="-Dfile.encoding=UTF-8 -Duser.language=zh -Duser.country=CN -Duser.timezone=Asia/Shanghai"
```

#### 2.3 MySQL容器配置优化：
```yaml
environment:
  MYSQL_CHARSET: utf8mb4
  MYSQL_COLLATION: utf8mb4_unicode_ci
  LANG: zh_CN.UTF-8
  LC_ALL: zh_CN.UTF-8
  TZ: Asia/Shanghai
command: >
  --character-set-server=utf8mb4 
  --collation-server=utf8mb4_unicode_ci
  --skip-character-set-client-handshake
```

#### 2.4 Nginx配置优化：
```nginx
charset utf-8;
charset_types text/css text/javascript application/javascript application/json application/xml text/xml text/plain;
add_header Content-Type "text/html; charset=utf-8" always;
```

### 3. 验证方法

#### 3.1 测试脚本：
```bash
# 运行测试容器
docker build -f test-dockerfile.Dockerfile -t test-chinese .
docker run --rm test-chinese
```

#### 3.2 预期输出：
```
简体中文：测试成功
繁體中文：測試成功
日本語：テスト成功
```

### 4. 重建和重启步骤

1. **停止现有服务**：
   ```bash
   docker-compose down
   ```

2. **清理旧镜像**：
   ```bash
   docker system prune -f
   ```

3. **重新构建镜像**：
   ```bash
   docker-compose build --no-cache
   ```

4. **启动服务**：
   ```bash
   docker-compose up -d
   ```

5. **验证中文显示**：
   - 访问 http://localhost
   - 检查登录页面、主页等中文内容是否正常显示

### 5. 注意事项

1. **构建缓存**：使用`--no-cache`确保重新安装所有依赖
2. **文件编码**：确保源代码文件保存为UTF-8编码
3. **数据库迁移**：MySQL已经配置为utf8mb4，确保现有数据兼容
4. **浏览器缓存**：测试时清除浏览器缓存或使用无痕模式

### 6. 故障排除

如果仍然出现乱码：

1. **检查容器环境变量**：
   ```bash
   docker exec -it structexam-frontend env | grep -i lang
   ```

2. **检查Nginx响应头**：
   ```bash
   curl -I http://localhost
   ```

3. **检查Java服务编码**：
   ```bash
   docker exec -it structexam-gateway bash -c "echo $JAVA_TOOL_OPTIONS"
   ```

4. **检查MySQL字符集**：
   ```bash
   docker exec -it structexam-mysql mysql -uroot -pstructexam123 -e "SHOW VARIABLES LIKE 'char%'; SHOW VARIABLES LIKE 'collation%';"
   ```

## 总结

通过以上修改，我们解决了Docker环境中中文乱码的问题：
1. 为所有容器设置了正确的中文locale环境变量
2. 为Java服务设置了UTF-8编码参数
3. 优化了MySQL的字符集配置
4. 加强了Nginx的编码响应头设置

这些修改确保了从前端到后端整个链路的字符编码一致性，中文显示应该能恢复正常。